#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * main - main function
 * @argc: argument count
 * @argv: argument vektor
 *
 * Return: Always (0)
 */
int main(int argc, char *argv[])
{
int i = 0;
for (i = 0; i < argc; i++)
{}
printf("%d\n", i - 1);
return (0);
(void)argv;
}
